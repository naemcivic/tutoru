<% if @appointment.valid? %>
  $("#appointment_errors").html("");
  $("#new_appointment")[0].reset();
  $(window.location.replace("/users/<%= current_user.id %>"))
<% else %>
  $("#appointment_errors").html("Appointment can't be created, choose the correct date and time.")
  $("#after-appointment").html("")
<% end %>
